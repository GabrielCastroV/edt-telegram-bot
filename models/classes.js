const newStudent = {
    weekDay: String,
    schedule: String('ex. from 9am to 12pm'),
    dayStart: Date,
    course: String,
    modality: String('presencial, semi, online'),
    availability: Boolean,
};

newStudent;